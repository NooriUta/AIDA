# ── Verdandi (Seiðr Studio) — environment variables ──────────────────────────
# Copy to .env.local and adjust for your environment.

# Proxied in vite.config.ts in dev; set for production builds.
SHUTTLE_URL=http://localhost:8080
CHUR_URL=http://localhost:3000
