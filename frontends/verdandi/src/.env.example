# ── Verdandi frontend — environment variables ─────────────────────────────────
# Copy to .env and adjust for your environment.

# Auth proxy (Chur BFF)
VITE_AUTH_URL=http://localhost:3000/auth

# GraphQL / lineage API
VITE_GRAPHQL_URL=http://localhost:3000/graphql
