CREATE OR REPLACE PROCEDURE TEST.PROC_HIER3 AS
    CURSOR c IS
        SELECT a.ACCOUNT_ID, LEVEL AS lvl
          FROM FIN.ACCOUNTS a
         START WITH a.PARENT_ACCOUNT_ID IS NULL
       CONNECT BY PRIOR a.ACCOUNT_ID = a.PARENT_ACCOUNT_ID;
BEGIN
    NULL;
END;
/
