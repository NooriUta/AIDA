{
  "name": "aida-root",
  "version": "0.0.0",
  "private": true,
  "description": "AIDA monorepo — dev tooling",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  }
}
